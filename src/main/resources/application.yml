# 🌟 Configuração Principal Tyler API
# Spring Boot YAML Configuration

spring:
  profiles:
    active: local

  application:
    name: tyler-api

  # 🔧 JPA/Hibernate (se necessário para Firestore)
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

# 📊 Actuator - Monitoramento
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

# 🚀 Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api
